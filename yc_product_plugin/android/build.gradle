group 'com.example.yc_product_plugin'
version '1.0'

buildscript {
    repositories {
        maven { url 'https://maven.aliyun.com/repository/public/' }
        maven { url 'https://maven.aliyun.com/repository/google/'}
        maven { url 'https://maven.aliyun.com/repository/jcenter/'}
        google()
        mavenCentral()
       flatDir{
           dirs project(':yc_product_plugin').file('libs')
       }
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:7.3.0'
    }
}

rootProject.allprojects {
    repositories {
        maven { url 'https://maven.aliyun.com/repository/public/' }
        maven { url 'https://maven.aliyun.com/repository/google/'}
        maven { url 'https://maven.aliyun.com/repository/jcenter/'}
        google()
        mavenCentral()

        // 配置依赖文件库
        flatDir{
            dirs project(':yc_product_plugin').file('libs')
        }
    }
}

apply plugin: 'com.android.library'

android {
    if (project.android.hasProperty("namespace")) {
        namespace 'com.example.yc_product_plugin'
    }

    compileSdkVersion 34

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    defaultConfig {
        minSdkVersion 26
        ndk {
            //noinspection ChromeOsAbiSupport
            abiFilters "armeabi", "armeabi-v7a", "arm64-v8a"//,"x86","x86-64"
        }
    }

    buildTypes {
        release {
            debuggable false

            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.debug
        }
    }

    dependencies {
        testImplementation 'junit:junit:4.13.2'
        testImplementation 'org.mockito:mockito-core:5.0.0'
    }



    testOptions {
        unitTests.all {
            testLogging {
               events "passed", "skipped", "failed", "standardOut", "standardError"
               outputs.upToDateWhen {false}
               showStandardStreams = true
            }
        }
    }

    // 导入 .so库
    sourceSets {
        main {
            jniLibs.srcDirs = ['libs']
        }
    }
}

dependencies {

    // 配置引入 aar
    api(name:"AliAgent-release-4.2.1",ext:"aar")
    api(name:"BmpConvert_V1.2.1-release",ext:"aar")
    api(name:"jl_bt_ota_V1.9.3-release",ext:"aar")
    api(name:"jl_rcsp_V0.5.2-release",ext:"aar")
    api(name:"JL_Watch_V1.10.0-release",ext:"aar")
    api(name:"ycbtsdk-release",ext:"aar")

//    api(name:"rtk-bbpro-core-1.6.1",ext:"jar")
//    api(name:"rtk-core-1.2.1",ext:"jar")
//    api(name:"rtk-dfu-3.3.8",ext:"jar")

    // jar 简写
    api fileTree(dir: 'libs', include: ['*.jar'])

    //解决Duplicate class kotlin.collections.jdk8.CollectionsJDK8Kt found in modules kotlin-stdlib-1.8.10
    api(platform("org.jetbrains.kotlin:kotlin-bom:1.8.0"))

    // 52832 的升级包
    api 'no.nordicsemi.android:dfu:2.4.0'

    // 引入Gson解析
    api 'com.google.code.gson:gson:2.8.6'
    api "com.squareup.okhttp3:okhttp:4.4.0"
    api 'org.eclipse.paho:org.eclipse.paho.client.mqttv3:1.1.0'

    implementation 'com.lai.weavey:dialog:2.0.1'
}
